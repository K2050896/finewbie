{% extends "homepage.jinja2" %}
{% block content %}

    <h1> These are {{ user_email }}'s portfolios: </h1>
    <ul> {{ port_id }} </ul>

    <form action = "{{ url_for('portfolios.optimize') }}" method = "POST">
        <input type="submit" name="optimize" value="OPTIMIZE!!!" />
    </form>

{% endblock %}